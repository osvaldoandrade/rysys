package br.com.codecompany.rysys.fgl.test.annotation;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;

public class TestAnnotations extends FglTestHelper {

	@Test
	public void testExtraction() {
		/*
		ClassWithAnnotations pojo = new ClassWithAnnotations();
		pojo.setOutFloatProperty(10.1f);
		pojo.setOutIntegerProperty(103);
		pojo.setOutStringProperty("ANDRE");
		List<Object> parameters = helper.extractFieldsAsList(pojo);
		Assert.assertEquals(10.1f, parameters.get(0));
		Assert.assertEquals(103, parameters.get(1));
		Assert.assertEquals("ANDRE", parameters.get(2));
		 */
	}
}
